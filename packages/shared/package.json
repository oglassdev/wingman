{
  "name": "@wingman/shared",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared protocol types and HTTP helpers for Wingman",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./port": "./src/port.ts",
    "./client": "./src/client.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
